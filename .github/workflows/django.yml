name: Django Test CI

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  test:
    using: 'docker'
    image: 'api/Dockerfile'
    steps:
    - name: Run Tests
      run: |
        python manage.py test
